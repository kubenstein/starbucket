{
  "name": "starbucket",
  "version": "1.0.1",
  "description": "Quickly create a dynamic, distributed and auto-syncing git server in a local network.",
  "scripts": {
    "dev:linter": "standard --verbose | snazzy",
    "dev:linter:noNpmError": "standard --verbose | snazzy || true",
    "dev:linter:fix": "standard --fix ; npm run dev:linter:noNpmError",
    "spec": "mocha spec/ --colors"
  },
  "author": {
    "name": "Jakub Niewczas",
    "email": "niewczas.jakub@gmail.com"
  },
  "bin": {
    "starbucket": "bin/starbucket"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kubenstein/starbucket.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=4.3.2"
  },
  "dependencies": {
    "fs-extra": "^3.0.0",
    "http-proxy": "^1.16.2",
    "node-discover": "^0.6.2",
    "node-git-server": "^0.1.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "snazzy": "^7.0.0",
    "standard": "^8.6.0"
  },
  "standard": {
    "globals": [
      "describe",
      "it",
      "beforeEach",
      "afterEach"
    ]
  }
}
