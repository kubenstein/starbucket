{
  "name": "starbucket",
  "version": "0.9.0",
  "description": "Quickly create a dynamic, distributed and auto-syncing git server in a local network.",
  "scripts": {
    "linter": "standard || true",
    "linter:fix": "standard --fix ; npm run linter",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Jakub Niewczas",
    "email": "niewczas.jakub@gmail.com"
  },
  "bin": {
    "starbucket": "bin/starbucket"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kubenstein/starbucket.git"
  },
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.16.2",
    "node-discover": "^0.6.2",
    "node-git-server": "github:gabrielcsapo/node-git-server"
  },
  "devDependencies": {
    "standard": "^8.6.0"
  }
}
